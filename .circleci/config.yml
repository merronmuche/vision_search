version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.9.0
    working_directory: ~
    steps:
      - checkout
      - run: pip install -r requirements.txt
      - run: python manage test
    