version: 2 # CircleCI version
jobs:
  build:
    machine:
      image: ubuntu-2022
    working_directory: ~/vision_search # Default working directory, 
    steps:
      - checkout
      - run: pip install -r requirements.txt
      - run: python manage test
    